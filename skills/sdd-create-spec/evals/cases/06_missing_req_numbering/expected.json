{
  "description": "Spec with functional requirements missing FR-XX numbering triggers a warning but is still valid",
  "expect_valid": true,
  "expect_rules": ["requirements-not-numbered"],
  "reject_rules": [
    "missing-title",
    "missing-section",
    "code-block-in-spec",
    "implementation-detail",
    "placeholder-text"
  ]
}
