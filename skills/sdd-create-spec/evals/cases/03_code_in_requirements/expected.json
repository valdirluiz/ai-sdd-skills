{
  "description": "Spec with code block and HTTP route in requirements fails validation",
  "expect_valid": false,
  "expect_rules": ["code-block-in-spec", "implementation-detail"],
  "reject_rules": [
    "missing-title",
    "missing-section",
    "placeholder-text"
  ]
}
